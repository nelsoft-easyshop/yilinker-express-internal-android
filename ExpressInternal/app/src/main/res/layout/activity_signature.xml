<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent" android:layout_width="match_parent" android:orientation="vertical">

    <TextView android:id="@+id/tvLabel"
        android:layout_height="?android:actionBarSize"
        android:layout_width="match_parent"
        android:gravity="center"
        android:text="@string/signature_good_condition" android:layout_alignParentTop="true" android:layout_marginTop="10dp"/>

    <ImageButton android:layout_margin="10dp" android:id="@+id/btnBack"
        android:layout_width="?android:actionBarSize"
        android:layout_height="?android:actionBarSize" android:src="@drawable/ic_back_arrow" android:background="@android:color/transparent" />


    <LinearLayout android:id="@+id/llButtons"
        android:layout_width="match_parent"
        android:layout_height="@dimen/bottom_button_height" android:orientation="horizontal"   android:layout_marginLeft="90dp"
        android:layout_marginRight="90dp" android:layout_alignParentBottom="true">

        <Button android:id="@+id/btnRating"
            android:layout_height="match_parent"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_marginBottom="20dp"
            android:text="@string/signature_rating" android:textColor="@color/white" android:background="@color/marigold"/>

        <Button android:id="@+id/btnSubmit"
            android:layout_height="match_parent"
            android:layout_width="0dp"
            android:layout_weight="1"

            android:layout_marginBottom="20dp"
            android:text="@string/signature_submit" android:textColor="@color/white" android:background="@color/orange_yellow"/>

    </LinearLayout>

    <com.yilinker.expressinternal.customviews.SignatureView
        android:layout_width="match_parent"
        android:layout_height="match_parent" android:id="@+id/viewSignature" android:layout_below="@+id/tvLabel" android:layout_above="@+id/llButtons"/>

</RelativeLayout>